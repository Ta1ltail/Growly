---
name: audit-2026-07
description: State of the July 2026 production code audit — decisions, env gaps, deferred items
metadata:
  type: project
---

Comprehensive audit/cleanup of project_101 (web/, shared/, supabase/) driven by CLAUDE.md. Started 2026-07-07.

**Migration strategy decision (needs user confirmation):** DB fixes were made by editing migration files 001/002/005 **in place** (not a forward 006_*.sql), per CLAUDE.md's "recreate-from-scratch" mandate. If migrations 001–005 are already applied to a live/prod DB, these must instead become a forward migration.

**Environment gaps:**
- `shared/` workspace has **zero test files** (`npm test` fails on it). Web has 11 test files / 163 tests, exercising shared logic via web re-exports.
- **Lint is broken**: `next/dist/compiled/babel/eslint-parser` dependency missing → eslint can't run. Verification relies on `tsc --noEmit` + vitest.
- typecheck: run per-workspace (`--workspace=shared` / `--workspace=web`); running both serially exceeds a 2min bash timeout.

**Fixed (verified):** notification RLS spoofing hole, SECURITY DEFINER revokes, public_profiles security_invoker + username_exists() RPC, friends UPDATE WITH CHECK, .or() + CSV injection, SyncProvider token-refresh sync-death (keyed on user?.id), getSession on hot push path + debounced stats snapshot, importJSON crash-safety, useToday midnight reschedule, avatar downscale, duplicate/missing indexes, seed idempotency, buildGameStats double streak-walk.

**Deferred (need design decision, NOT done):** merge resurrects deleted marks/habits (needs tombstones or day-level local authority — changes multi-device sync semantics); rarity constants duplicated across 5 files (xp/economy/rarity/achievements/celebrations); further console gating.
