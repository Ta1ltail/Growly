# Stale-snapshot refresh runs as a pg_cron job defined in
# migrations/001_schema.sql (cron.schedule 'refresh-stale-snapshots'), not as an
# edge function. The previous [functions.refresh-stale-snapshots] block pointed
# at ./functions/refresh-stale-snapshots/index.ts, which does not exist and
# broke `supabase start`; it was removed to keep the two mechanisms from
# duplicating and to let local startup succeed.
